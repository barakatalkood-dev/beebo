# URL of the backend API, including the /api suffix.
# Local dev: leave unset, it defaults to http://localhost:5000/api
# Production (Railway etc.): set to your deployed backend's public URL
VITE_API_URL=https://your-backend.up.railway.app/api
